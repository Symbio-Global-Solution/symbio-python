# 🐍 SYMBIO - Admin CLI (Python)

[cite_start]Aplicação de linha de comando (CLI) desenvolvida em Python para realizar operações CRUD (Create, Read, Update, Delete) diretamente no banco de dados Oracle do projeto SYMBIO[cite: 267].

**Disciplina Relacionada:** *Computational Thinking using PYTHON*

## ✨ Funcionalidades
* [cite_start]Menu interativo para navegação[cite: 284].
* CRUD completo para a tabela de `T_SYM_SKILL`.
* CRUD completo para a tabela de `T_SYM_VAGA`.
* [cite_start]Tratamento de exceções (try-except) para falhas de banco[cite: 286].
* [cite_start]Validação de entradas de usuário[cite: 285].
* [cite_start]3 Consultas diferentes com opção de **exportação para JSON**[cite: 290].

## 🛠️ Tecnologias Utilizadas
* Python
* `oracledb` (Driver oficial do Oracle)

## 🚀 Como Executar

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/](https://github.com/)[seu-usuario]/symbio-cli-python.git
    cd symbio-cli-python
    ```
2.  **Crie e ative um ambiente virtual:**
    ```bash
    python -m venv venv
    source venv/bin/activate
    ```
3.  **Instale as dependências:**
    ```bash
    pip install -r requirements.txt
    ```
4.  **Execute o programa principal:**
    ```bash
    python main.py
    ```
5.  Siga as instruções do menu no terminal.

## deliverables
[cite_start]A entrega desta disciplina inclui este código-fonte e um **documento PDF** contendo a especificação da solução e o link para o Vídeo Pitch[cite: 269, 277].
